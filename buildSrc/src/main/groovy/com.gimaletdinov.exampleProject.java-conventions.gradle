/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id 'java-library'
    id 'maven-publish'
}

repositories {
    mavenLocal()
    maven {
        url = uri('https://repo.maven.apache.org/maven2/')
    }
}

dependencies {
    implementation libs.spring.boot.starter.validation
    implementation libs.spring.boot.starter.data.jpa
    implementation libs.spring.boot.starter.security
    implementation libs.io.jsonwebtoken
    testImplementation libs.spring.boot.starter.test

    compileOnly libs.lombok.main
    annotationProcessor libs.lombok.main
    testCompileOnly libs.lombok.main
    testAnnotationProcessor libs.lombok.main
}

group = 'com.gimaletdinov.exampleProject'
version = '0.0.1-SNAPSHOT'
java.sourceCompatibility = JavaVersion.VERSION_11

publishing {
    publications {
        maven(MavenPublication) {
            from(components.java)
        }
    }
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
tasks.named('test') {
    useJUnitPlatform() // <5>
}
